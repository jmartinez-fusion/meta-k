apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-auth
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ms-auth
  template:
    metadata:
      labels:
        app: ms-auth
    spec:
      containers:
        - name: server
          envFrom:
            - secretRef:
                name: auth-secret
          image: jmartinezblanco/meta-ms-auth
